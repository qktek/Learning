/**
 * Toán tử gán
 * Toán tử      Ví dụ       Tương đương
 * =            x = y       x = y
 * +=           x += y      x = x + y
 * -=           x -= y      x = x - y
 * *=           x *= y      x = x * y
 * /=           x /= y      x = x / y
 * **=          x **= y     x = x ** y           
 */

// Tổng kết
// x++ tăng giá trị biến lên 1 và trả về giá trị trước khi tăng
// ++x tăng giá trị biến lên 1 và trả về giá trị sau khi tăng
// x-- giảm giá trị biến xuống 1 và trả về giá trị trước khi giảm
// --x giảm giá trị biến xuống 1 và trả về giá trị sau khi giảmm
// Trong video sau, chúng ta sẽ phân tích một cách chi tiết để hiểu nguyên lý của cách hoạt động trên.

/**Toán tử so sánh
 * ==   -> Bằng
 * !=   -> Không bằng
 * >    -> Lớn hơn
 * <    -> Nhỏ hơn.
 * >=   -> Lớn hơn hoặc bằng
 * <=   -> Nhỏ hơn hoặc bằng
 */

/**Boolean
 *  var isSucces true or false
 */

/**
 * if - else
 * 
 * if('bieu thuc dieu kien'){
 * điều kiện dúng
 * }else{
 * điều kiện bị sai
 * }
 */

/**
 * Những điều kiện False
 * 0
 * false
 * '' - ""
 * undefined
 * NaN
 * null
 * Ngoài những điều kiện này đều là true
 */

/**Toán tử logic
 * 1. $$    -> And 
 * 2. ||    -> Or
 * 3. !     -> Not
 * 
 * if(a > 1 && b > 1){
 *  console.log('Dieu kien dung')
 * }
 */

/** Kiểu dữ liệu trong Javascript
 * 1. Kiểu dữ liệu nguyên thủy - Primitive Data
 * - Number
 * - String
 * - Boolean
 * - Undefined
 *      var age;
 *      console.lgo(age)
 * - Null
 *      var isNull = null; //nothing
 * - Symbol
 *      var id = Symbol('id'); //unique
 * 2. Kiểu dữ liệu phức tạp - Complex Data
 * - Function
 *      var myFunction = function(){
 *          alert('Hi, Xin chao cac ban')     
 * }
 * goi function --> myFuction()
 * 
 * - Object
 *      var myObject = {
 *      name: 'Dinh',
 *      age: 39,
 *      address: 'BD'
 *      myFunction(){
 *      
 * }
 * };
 * 
 *      var myArray = [
 *      'JavaScript'
 *      'Python'
 *      'PHP
 * ];
 *  
 *       check --> console.log(typeof )   
 * 
 */

/**Toán tử so sánh P2 
 * ===
 * !==
 * example
 * var a = 1;
 * var b = '2';
 * console.log(a !==b);
*/

// Hiểu hơn về câu lệnh điều kiện
// và phép so sánh

/**
 * 1. Hàm?
 * -    Một khối mã
 * -    Làm 1 việc cụ thể
 * 2. Loại hàm
 * -    Built-in
 * -    Tự định nghĩa
 * 3. Tính chất
 * -    Không thực thi khi định nghĩa
 * -    Sẽ thực thi khi được gọi
 * -    Có thể nhận tham số
 * -    Có thể trả về một giá trị
 * 4. Tạo điều kiện đầu tiên
 * 
 */

